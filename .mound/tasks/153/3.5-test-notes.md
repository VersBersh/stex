# Test Notes — Task 153

## Test files

### `src/main/soniox-lifecycle.test.ts`
New `describe('pendingStartMs')` block with 8 tests:
1. Captures first non-final token's `start_ms`
2. Returns null when `onNonFinalTokens` receives empty array
3. Returns null when no tokens ever received
4. Final tokens clear `lastNonFinalStartMs` but NOT `pendingStartMs` (frozen snapshot)
5. `pendingStartMs` persists across full finalization cycle
6. Captures most recent non-final start (overwrites previous)
7. `resetLifecycle()` clears `pendingStartMs`
8. Uses first token's `start_ms` from multi-token batch

### `src/main/session.test.ts`
3 new tests in existing `describe('pause')` block:
1. Captures `pendingStartMs` on pause when non-final tokens exist
2. `pendingStartMs` is null on pause when no non-final tokens
3. `pendingStartMs` survives finalization during pause (frozen snapshot semantics)

## Acceptance criteria coverage
- [x] At pause, if unfinalized tokens exist, `pendingStartMs` is captured — tests 1, 8 (lifecycle); test 1 (session)
- [x] `pendingStartMs` survives across the pause period — tests 4, 5 (lifecycle); test 3 (session)
- [x] If no unfinalized tokens at pause, `pendingStartMs` is null — tests 2, 3 (lifecycle); test 2 (session)
- [x] Late-arriving final tokens during pause don't clear frozen snapshot — tests 4, 5 (lifecycle); test 3 (session)
- [x] Value exposed via `getPendingStartMs()` for resume-time consumption — getter tested throughout
