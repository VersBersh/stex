# Discovered Tasks

1. **DESIGN: Replace placeholder tray icon with branded icon**
   - Description: The current tray icon is a minimal grey square PNG embedded as base64. Design and create a proper branded icon (likely .ico for optimal Windows rendering) and update `src/main/tray.ts` to use it.
   - Why discovered: The task scope called for a placeholder icon. A real icon requires design work outside the scope of this implementation task.
