No spec updates required.

This is a pure test refactoring task — splitting one test file into multiple focused files. No production code, APIs, or specs are affected. The test runner (`vitest.config.ts`) already uses a glob pattern (`src/**/*.test.ts`) that will automatically discover the new test files.
