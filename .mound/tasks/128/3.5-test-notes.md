# Test Notes

## Test files modified

- `src/main/soniox-lifecycle.test.ts` — Added `onAudioLevel` mock to `createMockCallbacks()`, plus two tests
- `src/main/session.test.ts` — Added three tests

## What each test covers

### soniox-lifecycle.test.ts
- **"resets audio level to MIN_DB on disconnect"** — Verifies `onAudioLevel` callback is called with -60 when WebSocket disconnects
- **"resets audio level to MIN_DB on Soniox error"** — Verifies `onAudioLevel` callback is called with -60 when Soniox `onError` fires

### session.test.ts
- **"resets audio level to MIN_DB on pause"** — Verifies `AUDIO_LEVEL` IPC is sent with -60 when session pauses
- **"resets audio level to MIN_DB on stop"** — Verifies `AUDIO_LEVEL` IPC is sent with -60 when session stops
- **"resets audio level to MIN_DB on quick dismiss"** — Verifies `AUDIO_LEVEL` IPC is sent with -60 when quick dismiss fires

## Acceptance criteria not tested automatically

- Visual verification that the meter bar animates smoothly to zero (relies on existing CSS transition, not testable in unit tests)
