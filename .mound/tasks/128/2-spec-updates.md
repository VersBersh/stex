# Spec Updates

No spec updates required. This task is a bug fix that adds reset behavior to existing audio level monitoring. No new APIs, types, or contracts are introduced — only a new export of an existing constant (`MIN_DB`) and additional calls to existing callbacks/IPC channels at appropriate lifecycle points.
