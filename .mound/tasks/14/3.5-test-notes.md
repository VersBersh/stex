# T14: Test Notes

## Test files created

### `src/renderer/overlay/editor/editorBlockManager.test.ts` (extended)
- `getDocumentLength` — empty, multiple blocks, after clear
- `applyEdit — tail insertion` — creates user block after soniox, extends existing user block, creates on empty manager
- `applyEdit — mid-document edit` — marks soniox block modified, keeps already-modified, updates user block, insertion within block, deletion within block
- `applyEdit — cross-block edits` — select+replace spanning blocks, deletion removing a full block
- `commitFinalTokens after modification` — new soniox block after modified block, new soniox block after user block

### `src/renderer/overlay/editor/InlineEditPlugin.test.ts` (new)
- `findTextDiff` — insertion at end, insertion mid-text, deletion, replacement, no change, empty to non-empty, non-empty to empty, single char at beginning, replacement at beginning

## Acceptance criteria coverage
- **User can edit committed text during streaming** — covered by applyEdit mid-document tests + cursor preservation (untestable without DOM)
- **Incoming tokens do not displace cursor** — cursor preservation in TokenCommitPlugin is DOM/Lexical behavior; cannot unit test without jsdom+React setup
- **Edited blocks marked `modified: true`** — covered by applyEdit mid-document tests
- **Copy/select-all seamless** — inherent to Lexical; no custom logic needed
- **Cursor resumes tracking at end** — cursor-at-end detection is DOM/Lexical behavior; cannot unit test without jsdom

## Criteria not testable automatically
- Cursor preservation behavior (requires Lexical DOM environment)
- Cursor tracking resume when moved to end (requires Lexical DOM environment)
- Visual integration of edits with streaming (requires full E2E)
