# Test Notes — Task 68

## Test files created

- `src/main/error-classification.test.ts` — Unit tests for `classifyAudioError` (mic-denied, mic-unavailable, unknown) and `classifyDisconnect` (api-key, rate-limit, network, empty reason).
- `src/main/reconnect-policy.test.ts` — Unit tests for `getReconnectDelay` (initial delay, exponential growth, max cap, boundary).

## Coverage

- All branches of `classifyAudioError` (3 paths: access denied/permission, device not found/unavailable, fallback unknown).
- All branches of `classifyDisconnect` (3 paths: api-key/unauthorized/authentication, rate-limit/quota/too-many, default network).
- Reconnect delay at attempts 0-5, 10, 100 to verify exponential growth and cap.

## Not tested automatically

- Integration wiring (session.ts importing and using these modules) is already covered by existing `session.test.ts` and `session-reconnect.test.ts`.
