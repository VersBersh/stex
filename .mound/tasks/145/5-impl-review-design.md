**Verdict** — `Needs Fixes`

**Issues**
1. **Major — Hidden Coupling / Embedded Design**: [`TimestampedTextNode.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-f497115a/src/renderer/overlay/editor/TimestampedTextNode.ts#L18) adds timestamp metadata, but it does not encode what should happen to that metadata when the node is edited. The class only defines construction, getters, and JSON serialization; it leaves all mutation behavior to `TextNode`. Combined with per-token insertion in [`TokenCommitPlugin.tsx`](C:/code/draftable/stex/.mound/worktrees/worker-1-f497115a/src/renderer/overlay/editor/TokenCommitPlugin.tsx#L94), that means the core rule for this feature depends on Lexical internals rather than an explicit abstraction in your code. A reader cannot tell from the code whether edits preserve timestamps, partially preserve them, or intentionally drop them. That is a real design gap because the feature’s value is the metadata lifecycle after edits. Fix by making the policy explicit in code: either override the relevant mutation/splitting paths, or add a dedicated normalization/helper that intentionally downgrades edited timestamped nodes to plain `TextNode`s and expose an explicit `isEdited`/`hasStaleMetadata` API.

2. **Minor — Primitive Obsession / Semantic Coupling**: [`TimestampedTextNode.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-f497115a/src/renderer/overlay/editor/TimestampedTextNode.ts#L23) and [`TimestampedTextNode.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-f497115a/src/renderer/overlay/editor/TimestampedTextNode.ts#L89) require callers to pass both `text` and `originalText`, and [`TokenCommitPlugin.tsx`](C:/code/draftable/stex/.mound/worktrees/worker-1-f497115a/src/renderer/overlay/editor/TokenCommitPlugin.tsx#L101) passes the same value twice. That duplicates an invariant at every call site and makes it easy to construct inconsistent nodes later. Fix by encoding the invariant in the API: default `originalText` to `text`, or better, add a `fromToken(token)` factory so callers cannot mis-wire the fields.

3. **Minor — Encapsulation / Clean Tests**: [`TimestampedTextNode.test.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-f497115a/src/renderer/overlay/editor/TimestampedTextNode.test.ts#L86) and [`TimestampedTextNode.test.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-f497115a/src/renderer/overlay/editor/TimestampedTextNode.test.ts#L141) assert directly on `__startMs`, `__endMs`, and `__originalText`. That couples the tests to Lexical-style internal field names instead of the public behavior the node promises. It reduces refactorability without adding much confidence. Fix by asserting via the getters or via `exportJSON()` for serialization-specific checks.