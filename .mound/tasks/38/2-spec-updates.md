# Spec Updates

No spec updates required. This is a build configuration consistency fix that mirrors an existing pattern from `tsconfig.main.json` to `tsconfig.renderer.json`. No specs govern tsconfig structure.
