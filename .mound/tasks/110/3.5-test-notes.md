# Test Notes

No new test files created. This task removes dead code (the `disconnected` status value) and fixes a misleading test name. The existing test at `src/main/session-reconnect.test.ts:476` already tests the correct behavior — that pause is a no-op during reconnecting state. Only the test name and a comment are corrected.

## What existing tests cover

- `session-reconnect.test.ts` line 476: Tests that pause is a no-op after disconnect (when status is `reconnecting`)
- `session-reconnect.test.ts` line 486: Tests that resume is a no-op during reconnecting state

## Acceptance criteria coverage

- **Audit all references**: Done during exploration — all references identified in 1-context.md
- **Remove `disconnected`**: Verified by TypeScript type-check (compile error if any code still references `disconnected`)
- **Passing build and tests**: Verified by running build + test suite
