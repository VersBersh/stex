# Test Notes

## Test files created

### `src/main/audio-level-monitor.test.ts`
- `computeDbFromPcm16`: silence -> -60dB, max amplitude -> ~0dB, known RMS -> correct dB, empty buffer -> -60, negative samples, clamp quiet to -60
- `createAudioLevelMonitor`: returns smoothed value, averages over window, slides window, handles partial window, default window size

## Existing tests to update
- `src/main/settings.test.ts` — update `APP_SETTINGS_DEFAULTS` assertion to include `silenceThresholdDb: -30`
- `src/preload/index.test.ts` — add `'onAudioLevel'` to the `listenerMethods` array

## Acceptance criteria not testable automatically
- Visual appearance of volume meter bar (requires visual inspection)
- "Smooth" update animation (CSS transition, requires visual inspection)
- Draggable threshold in settings page (requires manual UI testing)
- Color coding of the volume bar (visual)
