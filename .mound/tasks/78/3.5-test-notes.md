# Test Notes — Task 78

## Test files created

1. **`src/main/session-ipc.test.ts`** — Tests for the extracted IPC registration module:
   - Registers all 6 IPC channels
   - Delegates pause/resume/dismiss-error/escape-hide to action callbacks
   - Opens settings window on open-settings IPC
   - Opens Windows mic settings on open-mic-settings IPC
   - Does not double-fire after re-registration (regression test for review issue #2)

2. **`src/main/soniox-lifecycle.test.ts`** — Tests for the extracted Soniox lifecycle module:
   - Creates and connects SonioxClient with settings
   - Starts audio capture on WebSocket connect
   - Forwards final/non-final tokens via typed callbacks
   - Calls onFinalizationComplete on finished event
   - Sets reconnecting status on network disconnect
   - Sets error status on non-reconnectable disconnect
   - Stops audio capture on disconnect
   - Sets error status on Soniox error
   - `resetLifecycle` disconnects client and clears reconnect state
   - `cancelReconnect` prevents pending reconnect from firing

## Existing test coverage

The existing test files (`session.test.ts`, `session-reconnect.test.ts`) continue to serve as integration tests for the full session orchestration through the unchanged public API. They should pass without modification.

## Acceptance criteria not testable automatically

None — all acceptance criteria are testable via the existing + new tests.
