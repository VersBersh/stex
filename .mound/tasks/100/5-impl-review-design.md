**Verdict** — `Needs Fixes`

**Issues**
1. Major — Hidden temporal coupling / incorrect follow-state check in the new auto-scroll logic. In [GhostTextPlugin.tsx#L12](C:/code/draftable/stex/.mound/worktrees/worker-1-e89472be/src/renderer/overlay/editor/GhostTextPlugin.tsx#L12) the code mutates layout with `controller.handleNonFinalTokens(tokens)` before deciding whether the user was “near bottom”, and in [TokenCommitPlugin.tsx#L61](C:/code/draftable/stex/.mound/worktrees/worker-1-e89472be/src/renderer/overlay/editor/TokenCommitPlugin.tsx#L61) the same happens after `editor.update(...)`. That means a single update that adds more than 50px of height can flip a user from “at bottom” to “not near bottom” before the check runs, so the viewport will stop following even though the user had not scrolled away. This violates the intended behavior and creates an order-dependent side effect. Fix by capturing `wasNearBottom` before the content mutation, then performing the scroll after the mutation if `wasNearBottom` was true. In `TokenCommitPlugin`, combine that with `cursorAtEnd`.

2. Minor — DRY / SRP / embedded design regression from duplicating low-level scroll-container knowledge in two plugins. [GhostTextPlugin.tsx#L15](C:/code/draftable/stex/.mound/worktrees/worker-1-e89472be/src/renderer/overlay/editor/GhostTextPlugin.tsx#L15) and [TokenCommitPlugin.tsx#L126](C:/code/draftable/stex/.mound/worktrees/worker-1-e89472be/src/renderer/overlay/editor/TokenCommitPlugin.tsx#L126) now repeat the same DOM traversal, threshold, and scroll assignment, and both assume `editor.getRootElement().parentElement` is the scroll container. That is a semantic coupling to the current JSX/CSS structure in [Editor.tsx#L40](C:/code/draftable/stex/.mound/worktrees/worker-1-e89472be/src/renderer/overlay/editor/Editor.tsx#L40) and [overlay.css#L97](C:/code/draftable/stex/.mound/worktrees/worker-1-e89472be/src/renderer/overlay/overlay.css#L97) that is not expressed by an abstraction. A future wrapper div or scroll-container change will require coordinated edits in multiple plugins. Fix by extracting a small shared helper such as `getEditorScrollContainer(editor)` / `scrollToBottomIfFollowing(...)` and a named constant for the threshold.