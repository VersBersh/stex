# Test Notes

No new tests written. This is a mechanical 1:1 replacement of `$getRoot().getTextContent()` with `$getDocumentText()` — the shared helper is literally a wrapper around the same call. Existing tests (particularly `multiParagraphIntegration.test.ts`) validate the text serialization contract. No behavioral change occurs.
