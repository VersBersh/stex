# Test Notes

## Test files created/modified

### `src/main/soniox-lifecycle.test.ts` (modified)

Extended `mockRingBufferInstance` to include `sliceFromWithMeta` mock. Added imports for `beginReplayPhase`, `sendReplayAudio`, `isInReplayPhase`. Added `describe('replay drain — zero-token timeout')` block with 4 tests:

1. **ends replay phase after 3 seconds when zero tokens received** — Covers the core acceptance criterion: silence-only replay completes via 3s timeout
2. **does not end replay via zero-token timer when final tokens arrive** — Verifies the zero-token timer is cancelled when tokens arrive, so normal drain flow isn't short-circuited
3. **ends replay via normal drain heuristic when tokens reach threshold** — Verifies existing drain heuristic still works
4. **10-second safety timeout works as ultimate fallback** — Verifies the safety timeout still triggers when tokens arrive but don't reach the drain threshold

## Acceptance criteria coverage

| Criterion | Test |
|-----------|------|
| Zero tokens within 3s → replay completes | Test 1 |
| Existing drain detection works normally | Tests 2, 3 |
| 10-second safety timeout remains | Test 4 |
| Tests cover zero-token scenario | Tests 1, 2 |
