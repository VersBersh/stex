# Spec Updates

No spec updates required. The context-refresh proposal (`spec/proposal-context-refresh.md`) already references "the sub-word merging step — see task 147" in its dirty-leaf model section, anticipating this work. The merger is an internal implementation detail that doesn't change any external contracts or spec-level interfaces.
