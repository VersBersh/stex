**Verdict** — `Needs Revision`

**Plan Issues**
1. Critical — Steps 5/7/8 assume replay can start at the exact `replayStartMs`, but the current buffer API cannot guarantee that. [`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L238) sets `connectionBaseMs = replayStartMs`, and [`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L335) replays `ringBuffer.sliceFrom(fromMs)`. In the real code, [`audio-ring-buffer.ts`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/src/main/audio-ring-buffer.ts#L33) returns whole chunks starting from the last chunk whose `startMs <= ms`, so replay audio can begin earlier than requested. That would skew all remapped token timestamps on connection B. Fix: either trim the first chunk to the exact replay boundary, or change the ring-buffer API to return `{ buffer, actualStartMs }` and use `actualStartMs` as the replay base.

2. Critical — Step 8’s drain detection uses a moving target, so replay completion can drift or never trigger. [`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L378) computes `replayDurationMs` from `ringBuffer.currentMs - connectionBaseMs`, while Step 6 keeps pushing resumed live audio into the same ring buffer during replay ([`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L300)). The current lifecycle already pushes every captured chunk into the ring buffer in `onAudioData` ([`soniox-lifecycle.ts`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/src/main/soniox-lifecycle.ts#L136)). Fix: capture an immutable replay end point when the replay slice is created/sent, and compare final-token coverage against that fixed boundary instead of `currentMs`.

3. Major — Step 9 converts committed text into ghost text before connection B is known to be usable, which can leave the editor de-committed on reconnect failure. The plan sends `SESSION_REPLAY_GHOST_CONVERT` immediately after `reconnectWithContext(...)` ([`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L459), [`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L465)), but the actual socket is only usable after WebSocket `open` ([`soniox.ts`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/src/main/soniox.ts#L64)). Fix: move ghost conversion into the successful `onConnected` path immediately before replay send, or add an explicit rollback path if the reconnect fails.

4. Major — Steps 9 and 10 disagree on replay send timing, and the Step 9 version is incompatible with the current `SonioxClient`. Step 9 says to call `sendReplayAudio` immediately and relies on client-side queuing ([`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L472)), but the real client drops audio unless the socket is already open ([`soniox.ts`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/src/main/soniox.ts#L111)). Step 10 later introduces the correct `onConnected` callback path ([`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L510)). Fix: make Step 10 the only supported flow, and remove the earlier immediate-send path and tests/comments that still assume queuing.

5. Minor — The new IPC contract is internally inconsistent. Step 1 defines `SESSION_REPLAY_GHOST_CONVERT` as sending `{ replayGhostStartMs: number }` ([`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L26)), but Step 2’s preload signature and Step 9’s sender both use a bare number ([`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L48), [`3-plan.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/3-plan.md#L465)). Fix: pick one payload shape and use it consistently across `ipc.ts`, preload, renderer, main, and tests.

**Spec Update Issues**
1. Major — The proposed replay-completion spec change is internally inconsistent and does not actually resolve the ambiguity it identifies. [`2-spec-updates.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/2-spec-updates.md#L19) says the spec should define explicit finalization/close as the replay-complete signal, then immediately says the plan will instead avoid finalization and use natural draining. The base spec still requires replay to be “sent” and then “finalized/drained” ([`proposal-context-refresh.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/spec/proposal-context-refresh.md#L222)). Fix: choose one concrete completion contract and specify it precisely.

2. Major — The spec updates miss the replay-slice boundary/timestamp contract that the plan now depends on. The proposal still treats replay as starting at `replayStartMs` with `connectionBaseMs = replayStartMs` ([`proposal-context-refresh.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/spec/proposal-context-refresh.md#L184)), but the real buffer API is chunk-based, not exact-start ([`audio-ring-buffer.ts`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/src/main/audio-ring-buffer.ts#L33)). Fix: update the spec to require exact trimming, or explicitly define an `actualReplayStartMs` returned by the ring buffer and use that for timestamp remapping and replay-drain tracking.

3. Major — The proposed “fire-and-forget” ghost-conversion IPC is underspecified for failure handling. [`2-spec-updates.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/.mound/tasks/154/2-spec-updates.md#L11) says main should send conversion IPC before replay audio and does not need a response, but once the renderer removes committed text there is no stated rollback if connection B fails or retries. The underlying handoff section is about a successful connection switch, not speculative mutation ([`proposal-context-refresh.md`](C:/code/draftable/stex/.mound/worktrees/worker-2-aacc23b6/spec/proposal-context-refresh.md#L133)). Fix: require conversion only after B is confirmed open, or specify an acknowledgement/rollback mechanism.