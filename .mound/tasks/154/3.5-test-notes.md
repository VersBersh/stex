# Test Notes — Task 154

## Test files created

### `src/renderer/overlay/editor/replayGhostConversion.test.ts`
- Removes clean tail nodes from replayGhostStartMs
- Suffix-match: truncates dirty node and folds suffix into ghost
- No nodes to remove: returns empty result
- All clean nodes removed
- Empty editor returns empty result
- Multiple clean tail nodes
- Non-suffix-match dirty node at replayGhostStartMs stays
- Round-trip: identical text is correctly ghosted and can be re-committed

### `src/main/audio-ring-buffer.test.ts` (added `sliceFromWithMeta` describe block)
- Returns actual chunk start timestamp
- Returns null when ms before oldest
- Returns null when ms >= currentMs
- Returns data from exact chunk boundary
- Returns null for empty buffer

## Acceptance criteria not automatically tested
- **Non-final replay tokens render into the replay ghost region**: This requires integration testing with the full GhostTextPlugin which renders via CSS. The mechanism is the same as existing live ghost text (CSS `--ghost-text-content` property), and covered by existing GhostTextPlugin tests.
- **Ghost text concept now supports two sources**: Verified structurally — both sources write to the same CSS property and are temporally exclusive (covered by replay phase buffering).
- **Re-committed tokens tagged `historic`**: The `historic` tag is applied via the existing `commitWords` path in TokenCommitPlugin, which is already tested.
