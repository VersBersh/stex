# Spec Updates

No spec updates required.

The task scope is limited to README.md, which already has the stale instructions removed. While `spec/decisions.md` (Decision #5) and `spec/models.md` (`audioInputDevice` field comment) still reference naudiodon/PortAudio, updating those specs is outside the scope of this documentation cleanup task and should be handled separately.
