# Test Notes — Task 150

## Test files modified

- `src/main/soniox-lifecycle.test.ts` — added 2 new `describe` blocks

## Tests added

### `applyTimestampOffset` (5 tests)
- Returns same array reference when offsetMs is 0 (short-circuit optimization)
- Offsets start_ms and end_ms correctly with non-zero offset
- Preserves all non-timestamp fields (text, confidence, is_final, speaker)
- Returns new array and new objects when offset is non-zero (no mutation)
- Handles empty array

### `connectionBaseMs` (3 tests)
- Initial connection passes final tokens through unchanged (same reference, offset 0)
- Initial connection passes non-final tokens through unchanged
- Reconnect passes tokens through unchanged (offset 0 after reconnect)

## Coverage notes

All acceptance criteria are testable and covered:
- `connectionBaseMs` tracked per connection: verified via lifecycle integration tests
- Token offset applied before consumer: verified by checking callback receives offset tokens
- Initial connection has offset 0: verified by same-reference check
- Single offset point: structural — applied in lifecycle callbacks only
- No regression: existing 33 tests still pass, plus session tests (64 tests)
