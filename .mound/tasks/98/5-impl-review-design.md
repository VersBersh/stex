**Verdict** — `Needs Fixes`

**Issues**
1. **Major — Hidden Coupling / Behavioral Regression:** [`ApiKey.tsx`](C:/code/draftable/stex/.mound/worktrees/worker-3-8be846f7/src/renderer/settings/pages/ApiKey.tsx#L14) and [`ApiKey.tsx`](C:/code/draftable/stex/.mound/worktrees/worker-3-8be846f7/src/renderer/settings/pages/ApiKey.tsx#L65) remove the ability to clear an existing API key. Once `hasKey` is true, the component only offers read-only display plus “Change”; in edit mode, “Save” is disabled for an empty `newValue`, so `onSettingChange('sonioxApiKey', '')` can never happen. That is a real regression from the previous freeform input and creates an unnecessary temporal rule: once a key exists, it must always be replaced with another non-empty key. It matters because users need a way to remove a stored secret, and because env-var fallback makes “there is a key” different from “a key is stored locally.” Fix by adding an explicit `Remove`/`Clear` action or allowing empty saves in edit mode, with copy that explains the env-var fallback case.

2. **Major — Embedded Design Principle / Naming / Semantic Coupling:** [`types.ts`](C:/code/draftable/stex/.mound/worktrees/worker-3-8be846f7/src/shared/types.ts#L46), [`settings.ts`](C:/code/draftable/stex/.mound/worktrees/worker-3-8be846f7/src/main/settings.ts#L61), and [`settings.ts`](C:/code/draftable/stex/.mound/worktrees/worker-3-8be846f7/src/main/settings.ts#L70) now use `AppSettings` for three different representations of `sonioxApiKey`: encrypted-at-rest, decrypted effective value, and masked renderer display value. The type and field name no longer reveal intent, so a reader cannot tell from the signatures whether `sonioxApiKey` is usable as a credential or only safe for display. That is semantic coupling across process boundaries and weakens the architecture’s self-documentation. Fix by separating the models, for example `StoredAppSettings`, `EffectiveAppSettings`, and `RendererSettings`, or by keeping `AppSettings` as the effective/main-process shape and introducing a distinct renderer field such as `maskedSonioxApiKey`.

3. **Minor — Hidden Coupling / Robustness:** [`settings.ts`](C:/code/draftable/stex/.mound/worktrees/worker-3-8be846f7/src/main/settings.ts#L19) and [`settings.ts`](C:/code/draftable/stex/.mound/worktrees/worker-3-8be846f7/src/main/settings.ts#L24) assume `safeStorage` is always usable. There is no `safeStorage.isEncryptionAvailable()` guard, so an unavailable platform key store becomes an implicit runtime precondition. On write, `encryptString` can fail; on read, the catch path treats any decrypt failure as “legacy plaintext,” which would misclassify an unreadable encrypted blob as a real API key string. Fix by making availability explicit: check `isEncryptionAvailable()`, fail with a clear settings-level error, or isolate crypto behind a small abstraction that can report unsupported/encryption-failed states distinctly from legacy migration.

4. **Minor — Clean Code / File Size:** [`settings.test.ts`](C:/code/draftable/stex/.mound/worktrees/worker-3-8be846f7/src/main/settings.test.ts#L1) is 411 lines, which crosses the stated 300-line smell threshold. It now mixes store mocking, IPC mocking, resolution rules, encryption behavior, and renderer-masking coverage in one file. That makes the test surface harder to scan and weakens the “architecture visible from code” goal even though the assertions themselves are reasonable. Fix by splitting it into focused files such as store behavior, IPC behavior, and API-key security/masking, with shared test helpers for the mocks.