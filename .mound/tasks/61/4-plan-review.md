**Verdict** — `Needs Revision`

**Plan Issues**
1. Major — Step 2: the proposed component test harness is not viable in the current test setup. [vitest.config.ts](C:/code/draftable/stex/.mound/worktrees/worker-7-2db63968/vitest.config.ts#L5) only includes `src/**/*.test.ts`, there is no DOM environment configured, and this repo does not currently have `jsdom`, `happy-dom`, `@testing-library/react`, or `react-test-renderer` installed in `node_modules`. [ghost-text.test.ts](C:/code/draftable/stex/.mound/worktrees/worker-7-2db63968/src/renderer/overlay/editor/ghost-text.test.ts#L1) is also a plain `.ts` utility test today, so “render `GhostTextPlugin` in a `LexicalComposer` and inspect styles” will not run as written. Fix: either extract the ghost-text style mutation into a pure helper and unit-test it under the existing node/Vitest setup, or explicitly add DOM-test infrastructure and update test discovery/file naming.

2. Major — Step 2: rendering `GhostTextPlugin` inside a bare `LexicalComposer` is insufficient, because the plugin reads `editor.getRootElement()` from Lexical, and the actual root element only exists when the editor mounts a `ContentEditable`. That happens in [Editor.tsx](C:/code/draftable/stex/.mound/worktrees/worker-7-2db63968/src/renderer/overlay/editor/Editor.tsx#L37), not in `GhostTextPlugin` itself; [GhostTextPlugin.tsx](C:/code/draftable/stex/.mound/worktrees/worker-7-2db63968/src/renderer/overlay/editor/GhostTextPlugin.tsx#L10) already guards on `!root`. Fix: if the test stays integration-style, render the full editor stack including `RichTextPlugin`/`ContentEditable`, or mock the Lexical editor object directly.

3. Minor — Step 2: adding integration-style React/Lexical tests into [ghost-text.test.ts](C:/code/draftable/stex/.mound/worktrees/worker-7-2db63968/src/renderer/overlay/editor/ghost-text.test.ts#L1) mixes a simple string-escaping unit test file with a very different class of test. Fix: either keep this file as utility-only and add a separate plugin-focused test file, or extract/test pure helpers so the existing file remains coherent.

**Spec Update Issues**
1. None. “No spec updates required” is correct. The existing specs already require this behavior: ghost text is ephemeral in [spec/models.md](C:/code/draftable/stex/.mound/worktrees/worker-7-2db63968/spec/models.md#L61), finalization removes ghost text in [spec/features/realtime-transcription.md](C:/code/draftable/stex/.mound/worktrees/worker-7-2db63968/spec/features/realtime-transcription.md#L30), and inline typing assumes ghost text clears when an utterance finalizes in [spec/features/inline-typing.md](C:/code/draftable/stex/.mound/worktrees/worker-7-2db63968/spec/features/inline-typing.md#L16).

