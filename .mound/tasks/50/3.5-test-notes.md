# T50: Test Notes

## Test files modified

- `src/main/window-construction.test.ts` — added `describe('overlay backgroundColor')` with 2 tests
- `src/main/settings-window.test.ts` — added `describe('settings backgroundColor')` with 2 tests

## What each test covers

- **Overlay light**: verifies `backgroundColor: '#ffffff'` when `resolveTheme()` returns `'light'`
- **Overlay dark**: verifies `backgroundColor: '#1e1e1e'` when `resolveTheme()` returns `'dark'`
- **Settings light**: verifies `backgroundColor: '#f5f5f5'` when `resolveTheme()` returns `'light'`
- **Settings dark**: verifies `backgroundColor: '#1e1e1e'` when `resolveTheme()` returns `'dark'`

## Untestable acceptance criteria

- "No visible white flash when opening windows in dark mode" — visual regression, cannot be tested in unit tests. Requires manual verification.
