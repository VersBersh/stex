# Test Notes — Task 79

## Test files modified
- `src/main/error-classification.test.ts` — restructured `classifyDisconnect` describe block

## What each test covers

### Standard close codes (code-based classification)
- `1000` (normal closure) → reconnectable network
- `1001` (going away) → reconnectable network
- `1006` (abnormal closure) → reconnectable network
- `1011` (internal server error) → reconnectable network
- Code precedence: `1006` with auth-like reason text → still reconnectable (code wins)

### Reason-text fallback (application-defined and ambiguous codes)
- `4001` + "invalid api key" → non-reconnectable api-key
- `4001` + "unauthorized" → non-reconnectable api-key
- `4001` + "authentication failed" → non-reconnectable api-key
- `4029` + "rate limit exceeded" → non-reconnectable rate-limit
- `4029` + "quota exceeded" → non-reconnectable rate-limit
- `4029` + "too many requests" → non-reconnectable rate-limit
- `1008` + "unauthorized" → non-reconnectable api-key (reason text needed for ambiguous code)
- `1008` + "policy violation" → reconnectable network (no false positive)
- `4000` + "something unexpected" → reconnectable network
- `4000` + "" → reconnectable network

## Acceptance criteria not tested automatically
None — all acceptance criteria are covered by the unit tests above.
