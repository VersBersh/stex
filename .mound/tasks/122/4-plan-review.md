**Verdict** — `Needs Revision`

**Plan Issues**
1. Major — **Step 2 / Step 3** only add timeout wording, but the stop/hide flow is still modeled as unconditional finalization. The implementation only finalizes on stop when the socket is connected and there are pending non-final tokens; otherwise it skips straight to cleanup/copy/hide. See [src/main/session.ts:151](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/src/main/session.ts#L151) and [spec/api.md:254](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/spec/api.md#L254). Fix: revise those steps so they update both “send empty frame” and “wait” to be conditional, not just timeout-aware.

2. Major — **Step 1** proposes “any unfinalized tokens are lost” after a pause timeout, which is not what the code guarantees. On pause, the app keeps the WebSocket open after timing out, and late final tokens are still forwarded if they arrive. See [src/main/session.ts:108](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/src/main/session.ts#L108) and [src/main/session.ts:48](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/src/main/session.ts#L48). Fix: remove that loss claim for pause, or explicitly limit it to stop/hide after disconnect.

3. Major — The plan says these edits make the feature specs consistent with `spec/api.md`, but `spec/api.md` does not currently state “5 seconds”; it only says “with a timeout.” See [spec/api.md:250](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/spec/api.md#L250) versus [src/main/session.ts:14](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/src/main/session.ts#L14). Fix: add a first step to update `spec/api.md` with the explicit 5-second timeout, or avoid introducing the numeric duration in dependent specs.

**Spec Update Issues**
1. Major — **`spec/api.md` is missing from the proposed spec updates.** The feature specs would cite Manual Finalization for a concrete 5-second timeout that the source-of-truth spec does not name. See [spec/api.md:248](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/spec/api.md#L248). Fix: update `spec/api.md` to state the 5-second timeout explicitly, or remove the specific duration from the feature specs.

2. Major — **`spec/features/realtime-transcription.md` and `spec/features/text-output.md` are only partially corrected.** Their stop/hide flows still imply “send empty frame, then wait” as an always-on path, which conflicts with the conditional stop behavior in [spec/api.md:254](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/spec/api.md#L254) and [src/main/session.ts:151](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/src/main/session.ts#L151). Fix: update those sections so finalization on hide is conditional.

3. Major — **`spec/features/inline-editing.md` proposed wording is inaccurate.** Saying timeout means “any unfinalized tokens are lost” overstates the pause behavior; after a pause timeout, the UI proceeds, but the connection remains open and late final tokens may still be delivered. See [spec/features/inline-editing.md:83](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/spec/features/inline-editing.md#L83) and [src/main/session.ts:112](/C:/code/draftable/stex/.mound/worktrees/worker-3-2fd2f13d/src/main/session.ts#L112). Fix: reword it to describe UI behavior after timeout without claiming token loss.