# Test Notes

## Test files modified

### `src/renderer/overlay/editor/InlineEditPlugin.test.ts`
Added `describe('multi-paragraph (\\n\\n separators)')` with 6 tests:
- Paragraph split (Enter key inserts `\n\n`)
- Paragraph join (Backspace removes `\n\n`)
- Text insertion in second paragraph
- Replacement across paragraph boundary
- Enter at end of text
- No-change detection with multi-paragraph text

### `src/renderer/overlay/editor/editorBlockManager.test.ts`
Added `describe('multi-paragraph support')` with 7 tests:
- Mid-document paragraph split: `\n\n` insertion marks block modified, updates text/length
- Tail paragraph break: creates user block with `\n\n`
- Typing after paragraph break: extends user block
- Paragraph join: removes `\n\n`
- Token commit after paragraph break: new soniox block created (previous is modified)
- `getBaseText` includes `\n\n` from non-trailing blocks
- Combined `applyEdit` + `replaceLastUserBlock` convergence: simulates both plugins firing

## Acceptance criteria coverage

| Criteria | Test coverage |
|----------|--------------|
| Block manager models paragraph separators | Mid-document split, tail break, getDocumentText tests |
| InlineEditPlugin text diff consistent across paragraphs | findTextDiff multi-paragraph tests |
| Enter key intercept removable without offset drift | Combined applyEdit + replaceLastUserBlock convergence test |
| Existing tests cover multi-paragraph scenarios | All new tests above |
| No regression in single-paragraph editing | Existing tests remain unchanged |

## Not automatically testable

- Full integration test with Lexical editor + both plugins running together (requires JSDOM/Lexical test setup — discovered task)
- Visual verification that Enter key works in the editor UI
