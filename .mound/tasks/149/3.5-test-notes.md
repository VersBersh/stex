# Test Notes

## Test files

- `src/main/soniox-lifecycle.test.ts` — added `describe('audio ring buffer')` block with 5 tests

## Coverage

1. **Creates ring buffer on connectSoniox** — verifies `AudioRingBuffer` constructor is called when a session starts
2. **Pushes audio chunks before sendAudio** — verifies `push` is called before `sendAudio` using call-order tracking
3. **Pushes every audio chunk** — verifies all chunks flow through the ring buffer
4. **Clears and nulls on resetLifecycle** — verifies `clear()` is called on session end, and a new instance is created on next session
5. **Persists across disconnect/reconnect** — verifies ring buffer is NOT cleared or re-created during reconnection

## Acceptance criteria not automatically tested

- "Ring buffer accumulates audio that can be retrieved via `sliceFrom` during a pause" — This requires integration testing with the real `AudioRingBuffer` class. The unit tests verify the wiring (push is called with each chunk), but an integration test against the real buffer would be needed to verify `sliceFrom` returns correct data. This is noted as follow-up per the plan review.
