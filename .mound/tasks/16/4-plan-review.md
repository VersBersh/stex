**Verdict** — `Needs Revision`

**Plan Issues**
1. `Critical` — Step 2 targets the wrong preload. The overlay window is created with [`src/main/window.ts:80`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/main/window.ts#L80), which loads [`src/preload/index.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/preload/index.ts), not [`src/main/preload.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/main/preload.ts). As written, adding `escapeHide()` to [`src/main/preload.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/main/preload.ts#L4) will not expose it to the actual overlay preload. This is compounded by the current mismatch where the renderer calls `window.electronAPI` in [`src/renderer/overlay/OverlayContext.tsx:113`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/renderer/overlay/OverlayContext.tsx#L113) and [`src/renderer/overlay/components/TitleBar.tsx:3`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/renderer/overlay/components/TitleBar.tsx#L3), while the active preload only exposes `window.api` in [`src/preload/index.ts:60`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/preload/index.ts#L60). Fix: first decide which preload the overlay actually uses, then put both the implementation and typings there, or consolidate the two bridges before adding `escapeHide()`.

2. `Major` — Step 7 clears document content for `onShow: 'fresh'`, but it does not clear Lexical history. [`clearEditor()`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/renderer/overlay/OverlayContext.tsx#L39) only clears the root and block-manager hooks; the history object is long-lived via [`createEmptyHistoryState()` in `Editor.tsx`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/renderer/overlay/editor/Editor.tsx#L31) and is only reset when final tokens commit in [`TokenCommitPlugin.tsx:53`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/renderer/overlay/editor/TokenCommitPlugin.tsx#L53). In fresh mode, the user could undo immediately and recover the previous session before any new tokens arrive. Fix: add a history-reset path for fresh-session clears, ideally in the editor layer so document state and undo state reset together.

3. `Minor` — Step 12 does not cover all of the surfaces it changes. The repo already has dedicated preload, tray, and window tests in [`src/preload/index.test.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/preload/index.test.ts), [`src/main/tray.test.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/main/tray.test.ts), and [`src/main/window-behavior.test.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/main/window-behavior.test.ts). The plan only adds `session.test.ts` cases, so the new preload API and tray flash behavior would be weakly verified. Fix: add test updates in the actual preload test file, plus tray tests for `flashTrayIcon()` and any routing tests needed for the new IPC path.

**Spec Update Issues**
1. `Major` — [`2-spec-updates.md`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/.mound/tasks/16/2-spec-updates.md) says the IPC-table edits in [`spec/architecture.md`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/spec/architecture.md#L118) are sufficient, but that is only true if the preload architecture is first made coherent. Right now the plan spans both [`src/preload/index.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/preload/index.ts) and [`src/main/preload.ts`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/src/main/preload.ts) while the actual window wiring points at one preload only. Fix: either add an architecture clarification about the overlay bridge surface, or revise the implementation plan to consolidate on one preload so the current spec delta really is enough.

2. `Minor` — The proposed spec updates omit the manual-copy wording mismatch between [`spec/hotkeys.md:21`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/spec/hotkeys.md#L21) and [`spec/features/text-output.md:23`](C:/code/draftable/stex/.mound/worktrees/worker-1-e8fe108c/spec/features/text-output.md#L23). The plan’s Step 10 explicitly implements “copy all when nothing selected,” but `text-output.md` still describes standard selected-text copy only. Fix: update `spec/features/text-output.md` to match the hotkeys spec, or drop Step 10 if that behavior is not intended.