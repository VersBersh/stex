# Struggles

No struggles. The feature was ~70% scaffolded (UI, types, IPC channels, preload bridge, settings persistence all existed). The remaining work was a one-line IPC handler fix, a test update, and a focus listener — straightforward to implement.
