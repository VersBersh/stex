# Test Notes

## Test files modified

- `src/main/first-run.test.ts` — Added one test case

## What each test covers

- **"does not open settings when API key is resolved from environment variable"** — Verifies that when `getSettings()` returns a key resolved from the `SONIOX_API_KEY` env var (simulated via mock), `showSettings()` is NOT called during first-run detection. This documents the contract that env-var-provided keys count as "available."

## Acceptance criteria coverage

| Criteria | Covered | Notes |
|----------|---------|-------|
| First-run detection checks resolved API key | Yes (new test + existing `settings.test.ts` line 150) | Mock-based in first-run test; real resolution tested in settings unit tests |
| Env var set = no API key prompt | Yes (new test) | Explicit test case for this scenario |
| Other first-run checks still run | Yes (existing tests at lines 98-122) | Existing tests verify all managers initialize regardless of API key state |
