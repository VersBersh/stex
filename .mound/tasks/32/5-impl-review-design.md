**Verdict** — `Approved with Notes`

**Issues**
1. **Major — DRY / Naming / Hidden semantic coupling:** The new test in [src/main/first-run.test.ts#L98](/C:/code/draftable/stex/.mound/worktrees/worker-8-07d10718/src/main/first-run.test.ts#L98) does not actually exercise environment-variable resolution. Because `getSettings()` is fully mocked in [src/main/first-run.test.ts#L46](/C:/code/draftable/stex/.mound/worktrees/worker-8-07d10718/src/main/first-run.test.ts#L46), this case is behaviorally identical to the existing “API key exists” test at [src/main/first-run.test.ts#L92](/C:/code/draftable/stex/.mound/worktrees/worker-8-07d10718/src/main/first-run.test.ts#L92): both just set `mockSettingsData.sonioxApiKey` to a non-empty string and assert `showSettings()` is not called. That gives no new coverage, but its name and comment imply the env-var path is verified. This matters because tests are part of the design documentation; a reader could wrongly conclude the `index.ts` to `settings.ts` integration is covered here when the real env-var behavior is only covered in [src/main/settings.test.ts](/C:/code/draftable/stex/.mound/worktrees/worker-8-07d10718/src/main/settings.test.ts). Suggested fix: either collapse these into a single test named around an “effective API key” existing, or stop mocking `getSettings()` for this case and let the test drive `process.env.SONIOX_API_KEY` through the real `getSettings()` path.

2. **Minor — Embedded design principle / spec clarity:** The edited spec sentence in [spec/features/system-tray.md#L18](/C:/code/draftable/stex/.mound/worktrees/worker-8-07d10718/spec/features/system-tray.md#L18) still says “On first launch”, but the implementation in [src/main/index.ts#L19](/C:/code/draftable/stex/.mound/worktrees/worker-8-07d10718/src/main/index.ts#L19) opens settings on any startup where the effective API key is absent, not only the very first run. Since this line was already being changed, leaving that phrase in place preserves a misleading architectural story. Suggested fix: reword the spec to describe startup behavior based on “when no API key is available” rather than “first launch”.

Other than that, the change is small, focused, and the modified files are comfortably below the 300-line smell threshold.