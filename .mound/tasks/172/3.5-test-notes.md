# Test Notes

## Test files modified

### `src/main/soniox-lifecycle.test.ts`
- Added `endReplayPhase` to import
- Added `describe('replay — state transitions and audio buffering')` block with 9 tests:
  1. `beginReplayPhase sets isInReplayPhase to true` — state transition
  2. `audio chunks are buffered during replay instead of sent to Soniox` — verifies ring buffer still receives data but sendAudio is not called
  3. `buffered live audio is flushed to Soniox when replay ends while connected` — verifies postResumeLiveBuffer flush
  4. `buffered live audio is discarded when replay ends while disconnected` — verifies buffer discard
  5. `sendReplayAudio handles null slice gracefully` — edge case: no audio in ring buffer
  6. `sendReplayAudio handles empty slice gracefully` — edge case: empty slice
  7. `sendReplayAudio updates connectionBaseMs from actualStartMs` — verifies token offset uses replay actualStartMs
  8. `disconnect during replay calls endReplayPhase` — verifies cleanup on disconnect
  9. `error during replay calls endReplayPhase` — verifies cleanup on error

### `src/main/session.test.ts`
- Added `isInReplayPhase` to import from `./soniox-lifecycle`
- Added `describe('replay flow')` block with helper `startAndPauseWithAudio()` and 5 tests:
  1. `sends ghost conversion IPC after connection B opens` — verifies SESSION_REPLAY_GHOST_CONVERT timing
  2. `sends replay audio via sendAudio after connection B opens` — verifies replay audio is sent
  3. `ghost conversion IPC fires before replay audio send` — verifies ordering via invocationCallOrder
  4. `effectiveReplayStartMs uses pendingStartMs when it is earlier than replayStartMs` — verifies Math.min computation with real ring buffer
  5. `replay-ineligible resume with editorWasModified does fresh reconnect without replay` — verifies no replay phase entered

## Acceptance criteria not automatically tested
- None — all acceptance criteria from the task description are covered by the tests above.
