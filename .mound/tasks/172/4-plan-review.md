**Verdict** — `Needs Revision`

**Plan Issues**
1. Major — Step 2 proposes an impossible renderer contract: `eligible: false` with `editorWasModified: true` and no blocked reason. The renderer currently derives `editorWasModified` as `replayAnalysis.eligible || replayAnalysis.blockedReason !== 'none'`, so `eligible: false` only becomes “modified” when `blockedReason` is not `'none'`. The proposed case in [3-plan.md](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/.mound/tasks/172/3-plan.md#L53) does not match the real producer in [OverlayContext.tsx](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/renderer/overlay/OverlayContext.tsx#L219), and the existing blocked-reason reconnect tests already cover the reachable non-replay path in [session.test.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/main/session.test.ts#L1247) and [session.test.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/main/session.test.ts#L1269). Fix: drop that case, or replace it with a reachable blocked-reason case and add the `isInReplayPhase() === false` assertion there.

2. Major — Step 2 test 5 says to “verify reconnect happens with replay config,” but the current `session.test.ts` harness does not expose that seam. `session.ts` passes replay metadata into the internal `reconnectWithContext(..., { replay, onReady })` call in [session.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/main/session.ts#L200), while the test only observes downstream `SonioxClient.connect` and renderer IPC; it does not spy on `reconnectWithContext` itself, as shown by the current imports in [session.test.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/main/session.test.ts#L139). Fix: either verify replay-path side effects that are observable (`SESSION_REPLAY_GHOST_CONVERT`, replay `sendAudio` after connection B opens), or split out a unit-style session test that mocks `./soniox-lifecycle` and asserts the `reconnectWithContext` arguments directly.

3. Major — Step 2 test 4 is underspecified and likely flaky as written. The plan says to prove `effectiveReplayStartMs = Math.min(...)` by checking that replay audio “starts from ~500ms,” but the real ring buffer slices from the chunk boundary at or before the requested time and returns `actualStartMs`, not a sample-trimmed slice; it also returns `null` if `ms >= currentMs`. See [3-plan.md](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/.mound/tasks/172/3-plan.md#L49) and [audio-ring-buffer.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/main/audio-ring-buffer.ts#L63). Fix: make `startAndPauseWithAudio()` deterministic with known chunk durations and enough total audio, then assert something exact: replay buffer length, exact `actualStartMs` effect via token offsets, or a precise `sendAudio` payload size.

4. Minor — Step 2 test 3 describes comparing “call index” between `mockOverlayWindow.webContents.send` and `mockSonioxInstance.sendAudio`. Raw call indices are per-mock and are not comparable across two different spies. The ordering is real in code, because `sendToRenderer(...SESSION_REPLAY_GHOST_CONVERT...)` happens immediately before `sendReplayAudio(...)` in the same `onReady` callback in [session.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/main/session.ts#L205), but the test needs a valid mechanism. Fix: use Vitest’s `invocationCallOrder`, or wrap both effects behind a single shared spy/log so cross-function ordering is observable.

**Spec Update Issues**
None. [2-spec-updates.md](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/.mound/tasks/172/2-spec-updates.md#L1) says no spec changes are needed, and that matches the current typed/runtime contract surface in [ipc.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/shared/ipc.ts#L1), [types.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/shared/types.ts#L67), [preload.d.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/shared/preload.d.ts#L37), and [preload/index.ts](C:/code/draftable/stex/.mound/worktrees/worker-1-51af2d0b/src/preload/index.ts#L103).