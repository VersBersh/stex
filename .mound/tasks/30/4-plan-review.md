**Verdict** — `Needs Revision`

**Plan Issues**
1. Major — Step 3 changes `toggleOverlay()` in [window.ts](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/src/main/window.ts:239) even though the stated bug is isolated to the raw `WINDOW_HIDE` IPC path in [window.ts](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/src/main/window.ts:149). `toggleOverlay()` is currently just a Window Manager visibility primitive, and the production entry points already use `requestToggle()` in [session.ts](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/src/main/session.ts:198). Making only the hide half of `toggleOverlay()` session-aware creates an asymmetric API and mixes Session Manager concerns into a lower-level helper without a production need. Fix: keep the implementation scoped to the `WINDOW_HIDE` handler unless there is a concrete caller that also needs lifecycle-aware toggle behavior.

2. Minor — Step 2’s test plan is not fully sound against the current test seam. `closeRequestHandler` is module state in [window.ts](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/src/main/window.ts:16), and `initWindowManager()` does not reset it in [window.ts](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/src/main/window.ts:138), so a fallback test for “no handler set” cannot safely assume a clean state after another test calls `setOverlayCloseHandler()`. Also, asserting only that the mock handler ran would not prove that `hideOverlay()` was skipped. Fix: isolate module state for the fallback case, or make the assertions observe a visible effect such as the window staying visible or hide persistence not running.

**Spec Update Issues**
1. Major — [2-spec-updates.md](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/.mound/tasks/30/2-spec-updates.md) says no spec changes are required, but [spec/architecture.md](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/spec/architecture.md:72) and [spec/architecture.md](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/spec/architecture.md:128) are already inaccurate for the current code and would remain inaccurate after this task. The hotkey/tray paths already go through `requestToggle()` in [hotkey.ts](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/src/main/hotkey.ts:3) and [tray.ts](C:/code/draftable/stex/.mound/worktrees/worker-2-ea046b9e/src/main/tray.ts:4), not directly to Window Manager, and after this change `window:hide` will be a session-aware hide request rather than a raw hide action. Fix: update `spec/architecture.md` so it describes visibility requests as routed through Session Manager orchestration, and clarify the `window:hide` IPC entry accordingly.