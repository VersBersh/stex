# T20 Test Notes

## Test files created

- `src/main/settings.test.ts`

## What each test covers

| Test | Acceptance Criteria |
|------|-------------------|
| `returns saved value when non-empty, ignoring env var` | AC #2: non-empty settings.json value takes precedence |
| `falls back to env var when saved value is empty` | AC #1: empty saved + env var set → uses env var |
| `returns empty string when neither saved nor env var set` | AC #3: neither set → `""` (first-run flow) |
| `treats whitespace-only saved value as non-empty` | Edge case: whitespace-only saved value still wins |

## Acceptance criteria not tested automatically

- AC #4 ("No secrets are written to disk or committed to git") — This is a design property, not a runtime behavior. The function is pure and returns a value without writing anything. Verified by code inspection.
