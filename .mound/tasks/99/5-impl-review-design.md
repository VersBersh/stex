**Verdict** — `Needs Fixes`

**Issues**
1. **Major — Hidden coupling / Single Responsibility:** Filtering `<end>` in [`soniox.ts` at line 133](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/src/main/soniox.ts#L133) changes message semantics from “replace current non-final state” to “no-op” when a response becomes empty after filtering, but the downstream ghost-text pipeline depends on receiving either final tokens or an explicit empty non-final list to clear speculative text. [`soniox.ts` lines 144-145](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/src/main/soniox.ts#L144) only emit non-final tokens when the list is non-empty; [`GhostTextPlugin.tsx` lines 11-16](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/src/renderer/overlay/editor/GhostTextPlugin.tsx#L11) clears only on `TOKENS_FINAL`, and [`ghost-text-utils.ts` lines 20-27](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/src/renderer/overlay/editor/ghost-text-utils.ts#L20) is prepared to clear on an empty non-final payload but never receives one here. The new test at [`soniox.test.ts` lines 484-499](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/src/main/soniox.test.ts#L484) locks in that no callback is emitted for an `<end>`-only response, which is exactly the behavior that can leave stale ghost text on screen after endpoint detection. Fix by preserving “message processed, current non-final set is empty” as an explicit event: emit `onNonFinalTokens([])` when `response.tokens` existed but `contentTokens` contains no non-finals, or otherwise centralize ghost-text clearing at the same abstraction level as marker filtering.

2. **Minor — Embedded design principle / Clean Code:** The spec change in [`api.md` lines 95-119](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/spec/api.md#L95) improves the marker-filtering story, but the same modified file still documents an endpoint and response field names that do not match the actual implementation in [`soniox.ts` lines 5, 16-19, 141](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/src/main/soniox.ts#L5). A reader of the architecture doc still sees `wss://stt.soniox.com/transcribe` and `audio_final_proc_ms`, while the code uses `wss://stt-rt.soniox.com/transcribe-websocket` and `final_audio_proc_ms`. That weakens the “code and module boundaries communicate design decisions” criterion because the touched spec remains unreliable in exactly the area being updated. Fix by aligning the doc with the current contract now, or clearly marking the pseudocode and examples as illustrative while keeping identifiers consistent.

3. **Minor — File size / Maintainability:** [`soniox.test.ts` is 533 lines](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/src/main/soniox.test.ts#L533), and this change adds more cases into the same monolithic file at [`lines 415-500`](C:/code/draftable/stex/.mound/worktrees/worker-4-836c2e15/src/main/soniox.test.ts#L415). The tests themselves are readable, but the file is already well past the 300-line smell threshold you asked reviewers to flag. That makes responsibilities blur across connection, parsing, lifecycle, stale-socket, and protocol-marker concerns. Fix by splitting protocol-filtering and message-handling scenarios into a dedicated suite, or by extracting shared response/socket helpers and separating behavioral areas into multiple test files.