- **Verdict** — `Needs Fixes`
- **Issues**
  1. **Critical — Hidden/temporal coupling: the app now starts headless and depends on future modules that do not exist yet.** The startup path in [src/main/index.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/index.ts#L5) only calls `initWindowManager()`, and the overlay is created with `show: false` in [src/main/window.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/window.ts#L57). At the same time, the previous `window-all-closed` quit path was removed, while [src/main/tray.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/tray.ts#L1) and [src/main/hotkey.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/hotkey.ts#L1) are still empty. In the current codebase, that means the process can launch with no visible window and no implemented way to reopen or quit it. This is a semantic-coupling regression: the window manager change only works if downstream tray/hotkey integration already exists, but nothing in the code enforces that dependency. Fix by landing tray/hotkey wiring in the same change, or keep a visible startup/quit path until those modules are real.
  2. **Major — Hidden coupling / incorrect state recovery: invalid saved positions are not actually corrected after the window already exists.** `getValidatedPosition()` in [src/main/window.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/window.ts#L14) is supposed to prevent off-screen restores, but `showOverlay()` applies bounds via `setBounds(bounds as Electron.Rectangle)` in [src/main/window.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/window.ts#L148) with only `width`/`height` when the position is invalid. That does not center the existing window; it preserves the current `x`/`y`. So if a user hides the overlay on a secondary monitor, disconnects that monitor, and shows it again, the window can remain off-screen even though validation returned `undefined`. The type-cast also hides that API mismatch. Fix by handling the two cases explicitly: set size, then either `setPosition(validX, validY)` or call `center()`/reset position when invalid.
  3. **Major — Side effects / temporal coupling: fade-in timers can fight hide/blur and leak state across visibility transitions.** The fade logic in [src/main/window.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/window.ts#L150) schedules chained `setTimeout`s that are never cancelled, while `hideOverlay()` in [src/main/window.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/window.ts#L175) and the blur handler in [src/main/window.ts](C:/code/draftable/stex/.mound/worktrees/worker-8-057a9595/src/main/window.ts#L77) can run concurrently. Because the callback mutates the module-global `overlayWindow`, an earlier `showOverlay()` can keep driving opacity after a hide, after a blur, or during a rapid hide/show sequence. That makes window state depend on call timing rather than explicit state transitions. Fix by capturing the specific window instance plus an animation token, and cancel any pending fade when hiding, blurring, or starting a new show animation.