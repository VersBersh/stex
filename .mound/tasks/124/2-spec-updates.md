# Spec Updates

No spec updates required. The project has spec files under `spec/` (architecture.md, models.md, api.md, etc.), but none of them define the Mound `.mound/config.toml` contract. The `[test_runner]` section is a Mound-system configuration, not a project-level spec concern.
