- drop down for input devices
- don't let us see the API key in settings once we've input it. Also, are we caching it? securely?
- bug: after first transcription we get an "<end>" tag
- bug: cursor does not follow the end of the line
- investigation: after we transcribe and then hit ctrl + shift + space it seems to take a while to finalize - even though it appears like the tokens are already all final. Is there any redundancy there?