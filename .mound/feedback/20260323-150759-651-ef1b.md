- after speaking the cursor did not move forward. It's supposed to track the end of the ghost text by default (until we click elsewhere)

- after the first text was returned from soniox, the input box scrolled a little and hid the text. We should avoid scrolling until necessary.

- can we investigate whether soniox API will accept some context - e.g. the start of text for which the recording will be a completion of